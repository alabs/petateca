Develop instalation
===================

Desde el root del repo Git

sudo easy_install -U pip
sudo pip install -U virtualenv
sudo pip install -U virtualenvwrapper
pip -E virtual install django
source virtual/bin/activate
python setup.py develop
